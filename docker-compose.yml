services:
  ai-finance-controller:
    build: .
    container_name: ai_finance_controller
    ports:
      - "8000:8000"
    volumes:
      - ./src:/app/src
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    restart: unless-stopped
